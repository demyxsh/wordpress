# Demyx
# https://demyx.sh

notifications:
  email:
    on_success: never
    on_failure: always

services:
  - docker

env:
  - DEMYX_USERNAME=demyx DEMYX_REPOSITORY=wordpress

before_install:
  - docker pull alpine
  - docker pull demyx/nginx
  - docker pull demyx/mariadb
  - docker pull demyx/utilities
  - docker network create demyx

script:
  - docker build --no-cache -t demyx/"$DEMYX_REPOSITORY" tag-latest
  - docker build --no-cache -t demyx/"$DEMYX_REPOSITORY":bedrock tag-bedrock
  - docker build --no-cache -t demyx/"$DEMYX_REPOSITORY":cli tag-cli
  - docker run -d --name=demyx_db --network=demyx -e DEMYX_DATABASE=demyx -e DEMYX_USERNAME=demyx -e DEMYX_PASSWORD=demyx -e DEMYX_ROOT_PASSWORD=demyx demyx/mariadb
  - docker run -d --name="$DEMYX_REPOSITORY" --network=demyx -v demyx_wp:/demyx -e DEMYX_DB_HOST=demyx_db -e DEMYX_DB_NAME=demyx -e DEMYX_DB_USER=demyx -e DEMYX_DB_PASSWORD=demyx demyx/"$DEMYX_REPOSITORY"
  - sleep 5
  - docker run --network=demyx --volumes-from="$DEMYX_REPOSITORY" --workdir=/demyx demyx/"$DEMYX_REPOSITORY":cli core install --url=http://localhost --title=demyx --admin_user=demyx --admin_password=demyx --admin_email=info@domain.tld --skip-email
  - docker run -dit --name=demyx_nx --network=demyx -v demyx_wp:/demyx -e DEMYX_WORDPRESS=true -e DEMYX_WORDPRESS_CONTAINER="$DEMYX_REPOSITORY" -e DEMYX_WORDPRESS_CONTAINER_PORT=9000 -e DEMYX_DOMAIN=localhost demyx/nginx

after_success:
  - echo "$DEMYX_PASSWORD" | docker login -u "$DEMYX_USERNAME" --password-stdin
  - docker push demyx/"$DEMYX_REPOSITORY"
  - docker push demyx/"$DEMYX_REPOSITORY":cli
  - bash travis.sh
